# Step 01 — Project Setup (Next.js + TypeScript + SCSS + Vitest + BiomeJS(linter))

## Goal
Initialize the project and prepare the base folder structure. ensure package.json is valid 
## Tasks
1. Create a new Next.js(with using pnpm) project with:
   - App Router
   - TypeScript enabled
   - SCSS support

2. Remove default boilerplate files.

3. Create folder structure:
```
/app
/components
/hooks
/utils
/styles
```

4. Add SCSS partials:
- `_variables.scss`
- `_mixins.scss`
- `_typography.scss`
- `_layout.scss`

5. Create `globals.scss` and import partials.

## Output
A clean Next.js project ready for development with using pnpm
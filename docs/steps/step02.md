# Step 02 — Define Types & Base Utilities

## Goal
Create TypeScript models and localStorage utilities.

## Tasks
1. Create `/utils/types.ts` :
   - Board
   - List
   - Card
   - Comment
add revision-number in these models   
2. Create `/utils/storage.ts`:
   - loadBoardState()
   - saveBoardState()

3. Create `/hooks/useLocalStorageState.ts`:
   - Generic hook for persistent state

## Output
Type-safe data models and reusable localStorage logic.
# Step 04 — Board Page & Layout

## Goal
Render the board page and provide state to components, using Trello theme and color palette.

## Tasks
1. Create `/app/board/page.tsx`.

2. Use `useBoardState()` to load board data.

3. Create components:
   - BoardHeader
   - ListContainer

4. Pass state + handlers via props or context.

## Output
A functional board page with placeholder UI styled with Trello theme and color palette.